{
  "name": "nhl-gm-backend",
  "version": "1.0.0",
  "description": "NHL GM Simulator - Backend API (Express + TypeScript)",
  "main": "server.ts",
  "scripts": {
    "start": "npx ts-node server.ts",
    "dev": "npx ts-node-dev --respawn server.ts",
    "build": "npx tsc server.ts --outDir dist-server --esModuleInterop --resolveJsonModule --moduleResolution node --target ES2020 --module commonjs --skipLibCheck",
    "start:js": "node dist-server/server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0"
  }
}
